# Neon PostgreSQL (recommended — auto-injected when Neon is linked to Vercel)
# DATABASE_URL이 있으면 Blob/KV보다 우선 사용됩니다.
DATABASE_URL=

# Vercel Blob (fallback — auto-injected when Blob store is linked)
BLOB_READ_WRITE_TOKEN=

# Optional: public | private (unset = try public first, then private)
# Vercel Blob store가 public이면 public, private이면 private 로 설정
BLOB_STORE_ACCESS=public

# Upstash Redis (recommended — auto-injected when Redis is linked to Vercel)
# KV가 연결되면 Blob보다 우선 사용됩니다.
KV_REST_API_URL=
KV_REST_API_TOKEN=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# One-time browser import at /admin/import (set any secret string)
DATA_IMPORT_KEY=
