# Vercel Blob (production on Vercel — auto-injected when Blob store is linked)
BLOB_READ_WRITE_TOKEN=

# Optional: public | private (auto-detect if unset)
BLOB_STORE_ACCESS=

# One-time browser import at /admin/import (set any secret string)
DATA_IMPORT_KEY=
